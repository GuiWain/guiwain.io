import { Story, Meta } from '@storybook/react'
import CardPost from '.'

export default {
  title: 'Components/CardPost',
  component: CardPost
} as Meta

export const Default: Story = () => <CardPost />
